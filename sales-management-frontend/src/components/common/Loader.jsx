export function Loader() {
  return (
    <div
      style={{
        padding: "10px 0",
        display: "flex",
        justifyContent: "center",
        fontSize: "0.8rem",
        color: "var(--text-muted)",
      }}
    >
      Loading...
    </div>
  );
}

export default Loader;
